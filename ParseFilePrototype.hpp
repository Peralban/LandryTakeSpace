/*
** EPITECH PROJECT, 2024
** ParseFilePrototype.hpp
** File description:
** ParseFilePrototype.hpp
*/

#pragma once

/**
 * @brief Parses the command line arguments.
 *
 * This function is responsible for parsing the command line arguments
 * provided to the program. It is expected to handle the arguments and
 * perform necessary actions based on them.
 *
 * @param ac The count of command line arguments.
 * @param av The array of command line arguments.
 */
void parseFile(int ac, char **av);
